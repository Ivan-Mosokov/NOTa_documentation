import React from 'react';
import { DuplicateIcon, Icon, PlusIcon, Duplicate24Icon } from './components';

// Компоненты, доступные в MDX файлах
export const mdxComponents = {
    DuplicateIcon,
    Icon,
    PlusIcon,
    Duplicate24Icon,
    // Можно добавить другие компоненты здесь
};

// Экспортируем компоненты для использования в MDX
export default mdxComponents;
